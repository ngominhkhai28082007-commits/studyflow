
# StudyFlow

StudyFlow is a full-stack study tracker with task-based focus sessions, stats, leaderboard, mascot progression, shop rewards, and realtime study rooms.

## Running the code

Run `npm i` to install the frontend dependencies.

Run `npm run dev` to start the frontend development server.

For the backend:

```bash
cd server
npm i
npm run dev
```
  
